# Coverage is uploaded from the unit test suite only (the functional suite needs a database that the
# code-coverage job does not provision). Keep the coverage status checks informational so they report
# numbers without blocking pull requests.
coverage:
    status:
        project:
            default:
                informational: true
        patch:
            default:
                informational: true

comment: false
